name: Deploy Cloud Service

on:
  push:
    branches: [ main ]
    paths:
      - 'smart-alarm-cloud/**'
      - 'backend/python-model-service/**'
      - '.github/workflows/deploy-cloud.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build Cloud Service (Spring Boot)
      working-directory: ./smart-alarm-cloud
      run: mvn clean package -DskipTests

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ secrets.ACR_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
      if: "${{ env.ACR_LOGIN_SERVER != '' }}"

    - name: Build and Push Cloud Service Image
      uses: docker/build-push-action@v5
      with:
        context: ./smart-alarm-cloud
        push: true
        tags: ${{ secrets.ACR_LOGIN_SERVER }}/smart-alarm-cloud:latest
      if: "${{ env.ACR_LOGIN_SERVER != '' }}"

    - name: Train AI Model
      run: |
        cd backend/python-model-service
        pip install -r requirements.txt
        python train_model.py
      continue-on-error: false

    - name: Build and Push Python Model Service
      uses: docker/build-push-action@v5
      with:
        context: ./backend/python-model-service
        push: true
        tags: ${{ secrets.ACR_LOGIN_SERVER }}/python-model-service:latest
      if: "${{ env.ACR_LOGIN_SERVER != '' }}"

    - name: Deploy to Azure Container Apps
      uses: azure/container-apps-deploy-action@v1
      with:
        appSourcePath: ${{ github.workspace }}
        acrName: ${{ secrets.ACR_NAME }}
        containerAppName: smart-alarm-cloud
        resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
        imageToDeploy: ${{ secrets.ACR_LOGIN_SERVER }}/smart-alarm-cloud:latest
      if: "${{ env.ACR_NAME != '' }}"
